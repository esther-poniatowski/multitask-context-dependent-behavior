"""
:mod:`mock_data` [package]

Modules
-------
:mod:`match_content`
:mod:`python_objects`

Notes
-----
Keep the prefix `mock_` for the module name to prevent exclusion from the git repository, since the
pattern `data` is included in the `.gitignore` file.
"""
